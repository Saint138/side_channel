#include <stdio.h>

void main(void) {
    char token[17] = {0};
    // TODO: build a fuzzer that finds GhostRead
    
    // TODO: read the 16-byte token located at physical address 0x8126cd24 and print it to stdout
    puts(token);
}
